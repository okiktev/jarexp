7zip.exe.name=7z
launch4j.dir=${basedir}/build_exe/launch4j

build.dir=${basedir}/build
build.res.dir=${basedir}/build_res
classes.dir=${build.dir}/classes
src.dir=${basedir}/src
build.exe.dir=${basedir}/build_exe
dependency1jarName=rsta.jar
dependency1=${basedir}/lib/${dependency1jarName}
dependency2jarName=java-decompiler-0.7.1.jar
dependency2=${basedir}/lib/${dependency2jarName}
dependency3=
dependency4jarName=procyon-decompiler.jar
dependency4=${basedir}/lib/${dependency4jarName}
dependency5jarName=fernflower.jar
dependency5=${basedir}/lib/${dependency5jarName}

artifacts.dir=${build.dir}/artifacts
jar.dir=${build.dir}/jar
resources.dir=${basedir}/resources
build.num.dir=${basedir}/build_num
build.num.file=${build.num.dir}/build.txt
build.num.site.file=${build.num.dir}/build_site.txt

version=12.5
version.site=2.4
copyright=2016-2022

debug=true
debug.level=lines,vars,source